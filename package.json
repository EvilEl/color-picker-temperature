{
  "name": "color-picker-temperature-monorepo",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "examples/*"
  ],
  "scripts": {
    "build:packages": "npm run build -w color-picker-temperature && npm run build -w color-picker-temperature-react && npm run build -w color-picker-temperature-vue && npm run build -w color-picker-temperature-astro",
    "build": "npm run build:packages && npm run build --workspaces --if-present --include-workspace-root=false",
    "check-types": "npm run build:packages && npm run check-types --workspaces --if-present",
    "test": "npm run build:packages && npm run test --workspaces --if-present",
    "test:browser": "npm run build:packages && npm run build -w @color-picker-temperature/example-astro && npm run test:browser -w color-picker-temperature",
    "pack:check": "npm run build:packages && node scripts/check-package-tarballs.mjs"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "@types/node": "^22.15.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "astro": "^5.13.0",
    "jsdom": "^26.1.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "typescript": "^5.9.0",
    "vite": "^7.1.0",
    "vitest": "^3.2.0",
    "vue": "^3.5.0",
    "vue-tsc": "^3.0.0"
  }
}
